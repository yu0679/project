<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.project_sample.dao.board.BoardDao">

<!-- 
<insert id="theme_insert" parameterType="boardvo">
    insert into theme(t_idx,t_name,mem_idx)
          values(seq_t_idx.nextVal,#{t_name},#{mem_idx});

</insert> -->

<!-- 총 피드수 -->
<select id="selectAllCount" resultType="int">
  SELECT COUNT(b_idx) FROM board
</select>



<insert id="Main_insert" parameterType="BoardVo" >
        insert into board(b_idx,b_subject,b_ip,b_start,b_end,t_idx,t_name,b_hit)
        values(seq_b_idx.NEXTVAL,
                  #{b_subject},
                  #{b_ip},
                  #{b_start},
                  #{b_end},
                  seq_t_idx.nextVal,
                  #{t_name},
                   0
               
        )
</insert>

  <select id="recentb_idx" resultType="int">
    select b_idx 
    from board
    where b_idx = (select max(b_idx) from board)
  </select>
<!-- 

  <select id="recent_main_Data" resultType="int">

    select b_idx 
    from board
    where b_idx = (select max(b_idx) from board)
  </select>


  <select id="all_data" resultType="BoardVo">

    select 
       b.b_idx  b_idx,
    from board b inner join  day d on b.#{b_idx}=d.b_idx
    inner join place p  on d.b_idx= p.b_idx
    inner join memo m on m.b_idx=d.b_idx
   
    order by b_idx desc

  </select>





  <update id="Board_update" parameterType="Map">
      update board set 
      b_ip = #{b_ip}, b_start = #{b_start}, b_end = #{b_end}, 
      b_content = #{b_content}, b_hit = #{b_hit}, d_idx = #{d_idx}, d_num = #{d_num}, 
      t_idx = #{t_idx}, t_name = #{t_name}, p_idx = #{p_idx}, memo_idx = #{memo_idx}, 
      mem_idx = #{mem_idx}
      where b_idx = #{b_idx}
  </update>
 -->



</mapper>